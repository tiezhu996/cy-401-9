-- MySQL creates the application database from docker-compose environment variables.
-- TypeORM entities synchronize the MVP tables: users, requirements, bids, contracts, operation_logs.
SET NAMES utf8mb4;
